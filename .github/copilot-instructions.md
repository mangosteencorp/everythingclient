# Repository instructions

Files matching `**/*.xcconfig` are protected configuration files. Never open,
read, summarize, quote, index, edit, or generate code suggestions for them.
Treat their contents as unavailable and ask the developer for a non-sensitive
placeholder when configuration context is required.
